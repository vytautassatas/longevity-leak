name: Quality

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Content Quality
        run: npm run quality:content

      - name: Directory Quality
        run: npm run quality:directory

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build
        run: npm run build
